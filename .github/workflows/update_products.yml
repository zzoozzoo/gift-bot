name: Update Top 16 Products

on:
  schedule:
    - cron: '0 1 * * 1'   # 매주 월요일 09:00 KST/TWN (UTC+8 → UTC 01:00)
  workflow_dispatch:        # 수동 실행 버튼

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install requests pytrends

      - name: Fetch products
        env:
          LINE_COOKIE: ${{ secrets.LINE_COOKIE }}
          LINE_CSRF_TOKEN: ${{ secrets.LINE_CSRF_TOKEN }}
        run: python scripts/fetch_top16.py

      - name: Commit result
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add static/data/top16.json
          git diff --staged --quiet || \
            git commit -m "chore: update top16.json $(date -u +%Y-%m-%d) [skip ci]"
          git push
